
REPORT : NLP PROJECT REPORT

Dataset used : Flickr8k (https://www.kaggle.com/datasets/adityajn105/flickr8k)
model_3.h4 - latest trained model
Used google collab as local system took alot of time to execute

Steps for execution:
1. Upload the data set in google drive and mount it to google collab
2. Run the nlp_project.ipynb in collab after downloading the below dependencies 
		!pip install Keras
		!pip install jupyterlab
		!pip install pillow
		!pip install NumPy
		!pip install tqdm
		!pip install TensorFlow
3. Execute the code